# Handover Notes — 2026-03-16

## Where We Left Off

**Session focus:** Release QA / Demo Readiness pass before presenting to Dr Panashe.

---

## Current State

- All 24 MVP features complete and passing
- EP001 (Claude Translation Escalation) shipped (PR #25, merged to main)
- QA hardening branch `codex/qa-demo-hardening` is **ready to merge** — contains two UI fixes and updated progress docs

**Branch: `codex/qa-demo-hardening`** (2 commits ahead of main)
- `fix(reader)`: disambiguate Google/Claude MT layers in reader tabs and record cards
- `chore`: add .playwright-mcp to gitignore; commit post-stage enhancements doc

---

## What Was Done This QA Session

### Bugs Fixed (in `codex/qa-demo-hardening`)

1. **TextLayerCard** (`src/components/records/TextLayerCard.tsx`)
   - Added a provider badge to the header row when `translation_provider` is set
   - Both Google MT and Claude MT cards are now visually distinct at a glance

2. **ScholarlyReader** (`src/components/records/ScholarlyReader.tsx`)
   - Layer tabs now show provider name for MT layers: "Machine Translation · Google Translate" / "Machine Translation · Claude (Anthropic)"
   - Selected layer metadata panel also shows a provider badge when `translation_provider` is set

### Code Quality Checks (all pass)
- Build: ✓ compiled in 2.1s, 0 errors
- TypeScript: ✓ no type errors
- ESLint: ✓ no warnings or errors

---

## What Still Needs To Be Done

### 1. Merge the branch
```bash
git checkout main
git merge codex/qa-demo-hardening   # or raise a PR
```

### 2. Seed demo data (BLOCKING for demo)
The Supabase database is **completely empty** — no projects, no records.
Before the demo with Dr Panashe, someone must walk through the full demo path once to create a clean demo record.

**Demo path to execute (creates the seed data):**
1. Go to `/projects/new` → create project "Panashe Archival Research"
2. Upload an archival PDF (any 19th-century newspaper page) with full metadata:
   - Archive, Publication Title, Language (xh/zu/st/nl), Date (YYYY-MM-DD)
3. Open the record → File Assets section → Extract Text (if PDF) or add a source OCR layer manually
4. Edit / Transcribe → save a corrected transcription layer
5. Generate Translation → Google MT draft (English)
6. Retry with Claude → Claude MT draft appears alongside Google draft
7. Add an annotation (scholarly note)
8. Add a flag (e.g. "uncertain")
9. Verify Scholar's Reader shows both MT tabs with distinct provider labels
10. Export citation (.txt or .json)
11. Check audit trail at `/projects/[id]/audit`

### 3. Verify Vercel production deployment
- Confirm latest `main` is deployed on Vercel (EP001 + provider label fixes)
- Confirm these env vars are set in Vercel:
  - `NEXT_PUBLIC_SUPABASE_URL` ✓ (user confirmed)
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY` ✓ (user confirmed)
  - `SUPABASE_SERVICE_ROLE_KEY` ✓ (user confirmed)
  - `GOOGLE_TRANSLATE_API_KEY` ✓ (user added this session)
  - `ANTHROPIC_API_KEY` ✓ (user added this session)

### 4. Live smoke test (not yet completed)
Playwright browser conflicted with the user's running Chrome instance — automated testing was blocked. The smoke test needs to be completed:
- Auth flow (sign in → dashboard → projects)
- Upload a record with metadata → record created with provenance
- Extract text or add OCR layer
- Generate Google MT
- Retry with Claude → both drafts appear with distinct labels
- Annotation, flag, search, citation export, audit trail

---

## Known Deferred Issues (post-demo)

| Issue | Severity | Notes |
|---|---|---|
| ScholarlyReader annotations strip hardcoded "None yet." | Low | Stub, not connected to real annotations. Annotations viewable on record detail page. |
| TranslationEditorForm title doesn't mention which provider draft is being corrected | Low | Functional, just not explicit |
| Storage path not aligned with canonical_ref | Low | Paths use `{projectId}/{recordId}/{filename}` — deferred to future feature |

---

## Key Files to Know

| File | What it does |
|---|---|
| `src/components/records/TextLayerCard.tsx` | Displays each text layer card — recently updated with provider badge |
| `src/components/records/ScholarlyReader.tsx` | Side-by-side reader — recently updated with MT tab disambiguation |
| `src/components/records/RetryWithClaudeSection.tsx` | EP001 Claude escalation trigger UI |
| `src/lib/actions/generate-claude-translation.ts` | EP001 Claude escalation server action |
| `src/lib/translation/translation-constants.ts` | Provider display labels, model constants |
| `docs/process/progress.md` | Session log |
| `claude-progress.txt` | Running progress and blockers |
| `feature_list.json` | Feature checklist (all 24 passed) |
| `plan/` | All implementation plans |

---

## Environment

- Dev server runs on port 3005 (3000 was in use during this session)
- `.env.local` has all 4 required keys set (Supabase + Google Translate + Anthropic)
- Supabase project: linked and running (remote, not local Docker)
