# Claude Progress Log
# This file tracks session-by-session progress
# Format: [Date] [Session] - Notes

## Current State
As of: 2026-03-16
Completed: F001–F022, F024 (23 of 24 features)
Day 1–5 complete. All P0 and all P1 features complete. One P2 feature remaining.
Next: F023 Related Text Suggestions (Day 5, P2) — only remaining feature

## Completed Features
- F001: Authentication and Role-Based Access — PASSED (2026-03-12)
- F002: Project and Team Management — PASSED (2026-03-12)
- F003: Manual Archive Upload Intake — PASSED (2026-03-12)
- F004: Upload Metadata Enforcement and File Naming — PASSED (2026-03-12)
- F005: Canonical Record Creation — PASSED (2026-03-13)
- F006: Ibali Source Integration — PASSED (2026-03-13)
- F007: NLSA Source Integration — PASSED (2026-03-13)
- F008: Wits Supplementary Source Intake — PASSED (2026-03-13)
- F009: Source File Viewer — PASSED (2026-03-13)
- F010: OCR and Source Text Acquisition Layer — PASSED (2026-03-13)
- F011: Text Layer Management — PASSED (2026-03-13)
- F012: Transcription Editor and Review Status — PASSED (2026-03-13)
- F013: Machine Translation Draft Generation — PASSED (2026-03-13)
- F014: Translation Editor and Correction Workflow — PASSED (2026-03-13)
- F015: Translation Memory — PASSED (2026-03-14)
- F016: Protected-Term and Glossary Rules — PASSED (2026-03-14)
- F017: Scholarly Side-by-Side Reader — PASSED (2026-03-14)
- F018: Annotation and Notes — PASSED (2026-03-14)
- F019: Uncertainty and Dispute Flags — PASSED (2026-03-14)
- F020: Search and Filter — PASSED (2026-03-14)
- F021: Citation Packet Export — PASSED (2026-03-14)
- F022: Corpus Trend View — PASSED (2026-03-14)
- F024: Admin Record Audit and Activity Trace — PASSED (2026-03-16)

## Blockers
- Final product name pending
- Readex permissions response pending

## Next Actions
- F023 Related Text Suggestions (Day 5, P2) — only remaining feature

## Known Limitations / Deferred
- Storage path still uses {projectId}/{recordId}/{filename} — not aligned with canonical_ref
  Storage reorganisation to use canonical_ref in path is deferred to a future feature
- Legacy F003 rows show 'LEGACY-{uuid}' sentinel in canonical_ref (not real refs)
  This cleanup is now part of F005 rather than a separate debt sprint

---

### Session 15 — 2026-03-13
- F014: Translation Editor and Correction Workflow — implemented via 3-agent parallel team, code reviewed (2 findings fixed), squash merged to main (PR #14)
- Migration `20260313000006`: `text_layers_insert_translator_correction` INSERT policy — scoped to `layer_type = 'corrected_translation'` and `translator` role via `source_records` + `project_memberships` join (mirrors existing contributor policy shape; no new SQL helpers)
- New server action `src/lib/actions/save-translation-correction.ts`: session-bound client (RLS enforced); validates `sourceLayerId` is a `machine_translation` layer; verifies source layer is active (rejects superseded MT layers via `supersedes_layer_id` reverse lookup); derives `record_id`, `project_id`, `language` server-side (language not accepted as client input); allows `project_admin`, `researcher`, `translator` roles (membership-only, no super_admin bypass); inserts `corrected_translation` with `source_layer_id` (provenance link, not supersession); audit log; `revalidatePath`
- New component `src/components/records/TranslationEditorForm.tsx`: mirrors `TranscriptionEditorForm`; `sourceLayerId` hidden input only; language displayed read-only (not submitted); `useActionState(saveTranslationCorrection, ...)`; auto-close on success via `useEffect` + submitted ref
- Modified `src/components/records/TextLayerCard.tsx`: `canCorrectTranslation` required prop; `showTranslationForm` state; "Correct Translation" button on active `machine_translation` layers; form state priority: TranslationEditorForm > TranscriptionEditorForm > CreateLayerVersionForm; action section visible when `canAddLayer || canCorrectTranslation`
- Modified `src/app/(app)/projects/[id]/records/[recordId]/page.tsx`: `canCorrectTranslation` membership-only (no super_admin initialisation — matches action boundary and F012/F013 pattern); includes `translator` role alongside `project_admin` and `researcher`; passed to all `TextLayerCard` instances
- Review P1 fix: superseded MT layer check added to server action — crafted requests against obsolete MT drafts rejected at the server boundary
- Review P2 fix: `canCorrectTranslation` initialised to `false` (not `super_admin` shortcut) — non-member super admins do not see correction affordance; read/write boundary consistent
- Both layers (MT draft + corrected_translation) remain active — no supersession, no cross-type linking; both accessible on record reopen
- Build passes clean (no warnings). All 5 PRD test steps satisfied. F014 PASSED.
- plan/2026-03-13-f014-translation-editor-correction-workflow.md written (addendums applied)

### Session 24 — 2026-03-16
- F024: Admin Record Audit and Activity Trace — implemented via 4-agent parallel team, 4 review rounds, squash merged to main (PR #23)
- Migration `20260314000008_f024_audit_logs`: `audit_logs` table — `actor_id ON DELETE RESTRICT` (preserves history on profile deletion); admin-only SELECT policy; NO INSERT policy (service role only — no user-session insert path)
- New: `src/lib/supabase/service.ts` — singleton service role client (`createClient<any>`), `import "server-only"` guard, used only from server-side code
- New: `src/lib/audit/log.ts` — `insertAuditLog({ projectId, actorId, actionType, recordId?, metadata? })`; full try/catch (never throws); checks `{ error }` from insert; service role client created internally (no supabase arg)
- Wired 14 action types: upload_record, import_ibali/nlsa/wits, add_text_layer, update_layer_status, extract_text, generate_translation, save_translation_correction, add_annotation, update_annotation, add_record_flag, update_record_flag, remove_record_flag
- New: `src/app/(app)/projects/[id]/audit/page.tsx` — server component; admin-only (isAdmin pattern); ?record=<UUID> filter with UUID regex validation; { error } from query logged; `formatMetadata()` renders compact suffix per action (e.g. "Updated layer status → approved"); limit 100
- Modified `src/app/(app)/projects/[id]/page.tsx`: "Activity Trace →" link (isAdmin)
- Modified `src/app/(app)/projects/[id]/records/[recordId]/page.tsx`: "Activity →" link to filtered audit view (isAdmin)
- Added `AuditLog` interface to `src/types/index.ts`
- Added `server-only` npm dependency
- Review iterations: (R1) tighten INSERT policy + UUID validation + metadata display; (R2) service role path to prevent forgery; (R3) super_admin non-member support (moot after R2); (R4) try/catch + server-only guard
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F024 PASSED.
- plan/2026-03-16-f024-admin-record-audit-activity-trace.md written

### Session 23 — 2026-03-14
- F022: Corpus Trend View — implemented via 2-agent parallel team (Agent A: utility + component; Agent B: page + dashboard), 2 P1 review findings fixed, squash merged to main (PR #22)
- Plan updated with 4 reviewer addendums before coding: no server action surface, F020 search escaping reused verbatim, drill-down mappings defined up front, AC4 corrected to 2-step drill-down
- New: `src/lib/trends/corpus-trends.ts` — plain async utility; two-phase search from F020 copied exactly; project-scoped; aggregates byYear/byPublication/byLanguage/bySource; `recordsUrl()` helper threads active source/language into all drill-down hrefs
- New: `src/components/records/TrendBarChart.tsx` — horizontal CSS bar chart; ILIZWI tokens; bar width = count/max * 100%; each bar is a Link; up to 15 items; empty state
- New: `src/app/(app)/projects/[id]/trends/page.tsx` — server component; URL-driven filters (q/source/language); GET form; 2×2 chart grid; all 4 empty/browse states
- Modified: `src/app/(app)/projects/[id]/page.tsx` — "Corpus Trends →" link added
- P1 fix: language options corrected to ISO codes (xh/zu/st/nl/en); were full names, didn't match stored data
- P1 fix: `recordsUrl()` helper preserves active source/language through all bucket hrefs
- V1 known limitation: byPublication bars use `q=<title>` (ILIKE, fuzzy) — records page has no exact publication_title filter param
- V1 known limitation: bars navigate to filtered records list, not directly to record detail (2-step, by design)
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F022 PASSED.
- plan/2026-03-14-f022-corpus-trend-view.md written (addendums incorporated)

### Session 22 — 2026-03-14
- F021: Citation Packet Export — implemented, all addendums applied, squash merged to main (PR #21)
- No DB migration required — all required data exists on source_records, text_layers, annotations, and profiles
- New shared module `src/lib/citation/citation-packet.ts`: `CitationPacket` type; `serializeToText()` (plain-text format with all sections); `serializeToJSON()` (indented JSON); `sanitizeCitationFilename()` (strips `/`, `:`, spaces from canonical_ref). No `"use server"` — safe to import from both server actions and client components.
- New server action `src/lib/actions/citation-export.ts`: `getCitationPacket(recordId)` — `requireAuth()` → query record by id → extract project_id from record → verify membership (super_admin bypass); queries text_layers (all, asc by created_at) and annotations with `profiles(display_name, email)` join; returns typed `CitationPacket` with stable `text_layers: []` / `annotations: []`
- New client component `src/components/records/CitationExportButton.tsx`: two buttons (.txt and .json); per-button loading state; inline error text (no new toast dependency); blob download via `URL.createObjectURL` + programmatic anchor + `revokeObjectURL`; filename sanitized from canonical_ref
- Modified `src/app/(app)/projects/[id]/records/[recordId]/page.tsx`: `CitationExportButton` rendered alongside Scholar's Reader link in the record header area; `recordId` and `canonicalRef` passed as props
- Addendums applied: server/client boundary clean (serializers in non-server shared module); real field names (`source_identifier`, `source_url`); auth in action itself (derives project_id from record); sanitized filenames; stable empty arrays in JSON; inline error UI; profiles join reuses existing pattern
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F021 PASSED.
- plan/2026-03-14-f021-citation-packet-export.md written with addendums

### Session 21 — 2026-03-14
- F020: Search and Filter — implemented via 2-agent parallel team, build/typecheck/lint all pass
- No DB migration required — all filterable fields already exist on source_records and text_layers
- New server component `src/components/records/RecordSearchFilters.tsx`: plain GET form (no "use client"); text search input, source select (ibali/nlsa/wits/manual_readex), language select (xh/zu/st/nl/en), status select (raw/in_review/approved), date range inputs; hidden flagged input preserves flagged filter across search; Clear filters link visible when any filter active; ILIZWI design tokens only
- Modified `src/app/(app)/projects/[id]/records/page.tsx`: extended searchParams (q, source, language, date_from, date_to, status); deterministic text search — text_layers ilike subquery (no project_id on text_layers — RLS scopes; main query .eq handles intersection), then single records query with .or(metadata fields + id.in(text_layer_ids)); structured filters applied as .eq/.gte/.lte; flagged filter preserved in same query chain (intersection semantics); result count displayed; empty state distinguishes no-records from no-match; RecordSearchFilters rendered above table
- All addendums from plan review applied: no "use client", deterministic search path, flagged intersection in same chain, canonical-ref navigation preserved, curated language list
- Known V1 limitations: records with only date_issued_raw won't match date range; languages outside curated list not directly filterable
- P1 fix: sanitize `q` before embedding in PostgREST `.or()` filter — strips `,()`. chars that break expression parsing
- P1 fix: flagged toggle link (`flaggedToggleHref`) rebuilt from all active params — no longer discards active filters
- Build, typecheck, lint all pass. All 8 acceptance criteria satisfied. F020 PASSED.
- Day 4 complete — all P0 features done (F017–F020).
- plan/2026-03-14-f020-search-and-filter.md written (addendums applied)

### Session 20 — 2026-03-14
- F019: Uncertainty and Dispute Flags — implemented via 4-agent parallel team, code reviewed (2 P1 findings fixed), squash merged to main (PR #19)
- Migration `20260314000007`: `flag_type_enum` (illegible, uncertain, disputed, needs_expert_review); `record_flags` table (project_id, record_id, text_layer_id nullable, flag_type, note nullable, created_by); two partial unique indexes (record-level and layer-level uniqueness); two standard indexes (record_id, project_id); `update_updated_at()` trigger reused; RLS SELECT (member OR super_admin), INSERT/UPDATE/DELETE membership-only — no super_admin bypass (matches action boundary). INSERT policy enforces created_by = auth.uid(), project_id ↔ source_records.project_id consistency, text_layer_id ↔ record_id consistency at DB layer.
- New server actions `src/lib/actions/record-flags.ts`: `addRecordFlag` — derives project_id server-side, validates text_layer_id belongs to same record, membership-only (no super_admin bypass), unique constraint violation surfaced as clear error; `updateRecordFlag` — updates note only (flag_type is immutable; change = delete + re-add); `removeRecordFlag` — DELETE with author-or-project_admin check; revalidates both record and records-list paths.
- New component `src/components/records/RecordFlagsPanel.tsx`: server component; amber left border (border-amber-400) distinct from annotation style; 4 flag type badges (illegible muted, uncertain amber, disputed red, needs_expert_review blue); optional note display; inline note editing via `?editFlag=<id>` search param; remove form (separate from edit); add form with flag_type select + target selector (record-level OR text layer); all mutation affordances gated on `canManageFlags`.
- Modified `src/types/index.ts`: added `RecordFlag` interface with `profiles?` join shape.
- Modified `src/app/(app)/projects/[id]/records/[recordId]/page.tsx`: added `canManageFlags` (any project member) and `canEditAllFlags` (project_admin only) — both derived membership-only with no super_admin bypass; flags fetched with profiles join; three server action wrappers; RecordFlagsPanel rendered after AnnotationsPanel.
- Modified `src/app/(app)/projects/[id]/records/page.tsx`: accepts `?flagged=true` query param; two-step server-side filter (fetch flagged record_ids from record_flags for project, then .in() filter on records query); "Show Flagged / Flagged Only" toggle button beside Upload Record.
- Review P1 fix: `canManageFlags`/`canEditAllFlags` derived membership-only — non-member super_admin cannot reach mutation affordances; UI/action boundary now consistent.
- Review P1 fix: add form and edit/remove controls gated on `canManageFlags` in panel — non-member super_admin sees flags read-only.
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F019 PASSED.
- plan/2026-03-14-f019-uncertainty-dispute-flags.md written.

### Session 19 — 2026-03-14
- F018: Annotation and Notes — implemented via 3-agent parallel team, code reviewed (3 findings fixed), squash merged to main (PR #18)
- Migration `20260314000005`: annotation_type_enum, annotations table, RLS SELECT (member OR super_admin), INSERT (any member OR super_admin), UPDATE (author OR project_admin OR super_admin); indexes on record_id + project_id; updated_at trigger
- Migration `20260314000006` (review hardening): INSERT policy tightened — added created_by = auth.uid(), project_id ↔ source_records.project_id consistency, text_layer_id ↔ record_id consistency at DB layer; added profiles_select_project_peers SELECT policy (project peers only — minimum scope for author attribution)
- New server actions `src/lib/actions/annotations.ts`: addAnnotation derives project_id server-side from record_id; validates text_layer_id belongs to same record_id; updateAnnotation derives context from annotation_id only; both return { error }, page wrappers handle redirects
- New component `src/components/records/AnnotationsPanel.tsx`: server component; border-l-2 border-historic marginalia style; 5 annotation type badges; display_name/email author attribution; linked layer label; edit link gated on authorship or canEditAll; add form visible to all project members
- Record detail page: searchParams wired; annotations fetched with profiles(display_name, email) join; canEditAllAnnotations derived separately (project_admin OR super_admin — NOT researcher); AnnotationsPanel rendered after Text Layers
- Review P0 fix: INSERT policy DB-layer enforcement of authorship + referential consistency
- Review P1 fix (canEditAll): canEditAllAnnotations flag — researchers excluded from edit-all path
- Review P1 fix (profiles): profiles_select_project_peers policy — author attribution now resolves correctly for annotations by other users
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F018 PASSED.

### Session 18 — 2026-03-14
- F017: Scholarly Side-by-Side Reader — implemented via 2-agent parallel team, code reviewed (1 P1 fix), squash merged to main (PR #17)
- New route: `src/app/(app)/projects/[id]/records/[recordId]/reader/page.tsx` — server component; combined tenancy guard (record + project_id); signed URL enrichment (same pattern as record detail page); derives `supersededIds` + `activeLayers` before passing to ScholarlyReader
- New component: `src/components/records/ScholarlyReader.tsx` — "use client"; full-height flex layout (top strip, left panel, right panel); default layer selection by priority (corrected_translation > machine_translation > corrected_transcription > source_transcription > source_ocr); asset switcher for multi-asset records; PDF iframe for trusted origins (Supabase, NLSA, Ibali); "open in new tab" fallback for non-PDF or untrusted-origin assets (matches FileViewerSection behavior); annotations placeholder strip; layer tabs with type badges + status badges; token-based styling only (no hex literals)
- Modified: `src/app/(app)/projects/[id]/records/[recordId]/page.tsx` — "Scholar's Reader →" link button added after h1; conditional on `canOpenReader` (≥1 asset AND ≥1 layer)
- Addendums applied: token-only styling; default layer priority; open-in-new-tab fallback; combined tenancy guard; annotations strip compact at 1280×800
- Review P1 fix: reader was passing full text_layers history (including superseded) — now filters to active layers only via same supersededIds derivation as record detail page
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F017 PASSED.
- plan/2026-03-14-f017-scholarly-side-by-side-reader.md written (addendums + review findings recorded)

### Session 17 — 2026-03-14
- F016: Protected-Term and Glossary Rules — implemented via 4-agent parallel team, reviewed (4 findings fixed), squash merged to main (PR #16)
- Migration `20260314000003`: `glossary_rules` table — `rule_type` CHECK (4 enum values), `approved_translation` NOT NULL CHECK when `rule_type='approved_translation'`, partial unique index `(project_id, lower(term), language) WHERE active=true` (case-insensitive deduplication), lookup index `(project_id, language, active)`, RLS SELECT (any project member or super_admin), INSERT/UPDATE (project_admin or super_admin)
- Migration `20260314000004` (review fix): expanded SELECT policy to include `super_admin`, matching app-layer access model used throughout
- New server actions `src/lib/actions/glossary-rules.ts`: `createGlossaryRule` (FormData-compatible, validates approved_translation required for that rule_type, deduplication error surfaced), `updateGlossaryRule` (partial-update safe — only clears approved_translation when rule_type is explicitly changed away from it, preserves existing value on deactivation to avoid CHECK violation)
- New page `src/app/(app)/projects/[id]/glossary/page.tsx`: server component, all members can view, project_admin/super_admin see create form + edit + deactivate; `?edit=<id>` param surfaces inline edit form with pre-filled values; create/edit/deactivate errors redirect back with query param error banners; deactivate-only workflow never violates CHECK constraint
- Modified `src/app/(app)/projects/[id]/page.tsx`: Glossary nav link visible to all members, "(view)" suffix shown for non-admins
- Modified `src/app/(app)/projects/[id]/records/[recordId]/page.tsx`: source layer resolved by priority (corrected_transcription → source_transcription → source_ocr); glossary rules fetched only when translation layer exists; whole-word tokeniser replaces substring search (split on whitespace, strip edge punctuation via Unicode-aware regex, exact token comparison — no false positives like "ink" matching "thinking"); Glossary Matches panel with rule type badges rendered inline above layer list
- Added `GlossaryRule` interface to `src/types/index.ts`
- Review findings fixed: (P0) partial-update CHECK violation on deactivate; (P1) edit UI missing; (P1) super_admin excluded from SELECT policy; (P1) substring matching causing false positives
- Build, typecheck, lint all pass. All 6 PRD test steps satisfied. F016 PASSED.
- plan/2026-03-14-f016-glossary-controlled-vocabulary.md written

### Session 16 — 2026-03-14
- F015: Translation Memory — implemented via 3-agent parallel team, PR #15 open for review
- Migration `20260314000001`: `translation_memory_entries` table — UNIQUE on `created_from_text_layer_id` (duplicate guard), indexes `tm_entries_lookup_idx (project_id, target_language, source_segment)` and `tm_entries_browse_idx (project_id, source_language, target_language)`, RLS SELECT (any project member) + INSERT (project_admin/researcher/translator), no UPDATE/DELETE (append-only V1)
- New server action `src/lib/actions/get-translation-memory-suggestions.ts`: resolves source segment via `MT.source_layer_id → source_layer.content` (not record-level active layer); exact-match query on (project_id, target_language, source_segment); returns ≤5 results with `canonical_ref` provenance; any failure returns `[]` silently — editor cannot be blocked by TM lookup
- Modified `src/lib/actions/save-translation-correction.ts`: step 2 select extended to include `source_layer_id` + `content`; step 8b non-blocking TM capture — resolves source transcription layer via `sourceLayer.source_layer_id`, inserts into `translation_memory_entries`; wrapped in try/catch; failure logged only, never returned as error
- Modified `src/components/records/TranslationEditorForm.tsx`: added `mtLayerId` prop; controlled textarea via `useState(sourceContent)`; TM suggestions fetched on mount (fire-and-forget); suggestions panel above textarea when results exist; "Use this" sets textarea value; per-suggestion dismiss (×) via `dismissedIds` Set; panel hidden when empty or on fetch failure
- Modified `src/components/records/TextLayerCard.tsx`: passes `mtLayerId={layer.id}` to `TranslationEditorForm` (only rendered when `layer.layer_type === "machine_translation"` so `layer.id` is the MT layer ID)
- Added `TranslationMemoryEntry` type to `src/types/index.ts`
- Addendums applied: source segment via MT chain; exact-match-only negative test in verification; full interaction path tested (dismiss, edit-after-accept, failure-tolerant); UNIQUE duplicate guard; lookup index shape matches query; canonical_ref in provenance
- TypeScript clean, lint clean (replaced `any` with typed `RawEntry` in suggestions action)
- plan/2026-03-14-f015-translation-memory.md written (addendums applied)
- F015 PASSED.

### Session 14 — 2026-03-13
- F013: Machine Translation Draft Generation — implemented via 3-agent parallel team, code reviewed (3 findings fixed), squash merged to main (PR #13)
- Migration `20260313000005`: `source_layer_id UUID` (self-referencing FK, NO ACTION — provenance is permanent) + `translation_provider TEXT` added to `text_layers`; `enforce_text_layers_immutability()` trigger extended to guard both new fields with `IS DISTINCT FROM`
- New module `src/lib/translation/translation-constants.ts`: `TARGET_LANGUAGE_ALLOWLIST`, `TargetLanguage`, `PROVIDER_DISPLAY_LABELS` — safe for client import; canonical display label source
- New module `src/lib/translation/google-translate.ts`: `translateWithGoogle()` with `SUPPORTED_SOURCE_LANGS` allowlist (African + European/colonial corpus languages); omits `source` param when language null or unrecognized (auto-detect path, not an error); `PROVIDER_NAME`; re-exports constants from `translation-constants.ts`; server-only
- New server action `src/lib/actions/generate-translation.ts`: membership-only auth (no super_admin bypass); source layer priority selection (`corrected_transcription > source_transcription > source_ocr`), tie-break by `created_at DESC`; blocks if any active MT layer exists including manually created null-provenance rows; validates API key before call; inserts with `source_layer_id` + `translation_provider`; audit log; `revalidatePath`
- New component `src/components/records/GenerateTranslationSection.tsx`: client component, `useActionState`, silent role gate (`canGenerate=false` → null), info notices for blocked states (no eligible layer / MT already exists), target language `<select>`, success shows layer ID
- Modified `src/app/(app)/projects/[id]/records/[recordId]/page.tsx`: `hasEligibleLayer` + `hasActiveMtLayer` flags computed server-side; `GenerateTranslationSection` rendered between `ExtractTextSection` and `AddTextLayerForm` inside `canAddLayer` gate
- Modified `src/components/records/TextLayerCard.tsx`: imports `PROVIDER_DISPLAY_LABELS` from `translation-constants.ts`; shows provider label in metadata row when present
- `.env.example`: `GOOGLE_TRANSLATE_API_KEY=` documented
- Review P1 fix: blocked-regeneration message no longer suggests unsupported workflow — now reads "An active machine translation draft already exists for this record. Regeneration is not available in this version."
- Review P2 fix: `PROVIDER_DISPLAY_LABELS` and `TARGET_LANGUAGE_ALLOWLIST` extracted to `translation-constants.ts`; client components import from there; `google-translate.ts` server-only boundary enforced by comment
- Review P3 fix: invalid `targetLanguage` now returns an explicit error (lists supported codes) instead of silently coercing to `"en"`
- TypeScript type extended: `TextLayer.source_layer_id: string | null`, `TextLayer.translation_provider: string | null`
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F013 PASSED.
- plan/2026-03-13-f013-machine-translation-draft.md written (addendums applied)

### Session 13 — 2026-03-13
- F012: Transcription Editor and Review Status — implemented via parallel subagent team (5 tasks, Tasks 1+2 parallel, 3+4 parallel, 5 sequential), code reviewed (2 findings, 1 fix commit), squash merged to main (PR #12)
- Migration `20260313000004`: `enforce_text_layers_immutability()` BEFORE UPDATE trigger guards `id` + 8 other immutable fields with `IS DISTINCT FROM` (null-safe for `language`, `supersedes_layer_id`); `text_layers_update_status` UPDATE RLS policy — membership-only (no super_admin bypass; consistent with membership-only SELECT policy)
- New server action `updateLayerStatus`: reads `layerId`/`newStatus` from formData; validates against full `VALID_LAYER_STATUSES` enum; derives `record_id`/`project_id` from DB via join (no client-trusted routing); direct membership-only permission check (not `assertLayerPermission` — avoids super_admin boundary mismatch with SELECT policy); UPDATE payload contains only `{ status }` field; audit log + revalidatePath
- Extended `addTextLayer`: accepts optional creation-time `status` — absent → `raw` (backward compat); present + invalid → error (not silent coerce, per addendum); `VALID_CREATION_STATUSES = ["raw", "reviewed", "needs_expert_review"]`
- New component `TranscriptionEditorForm`: props `recordId`, `sourceContent`, `sourceLanguage`, `onClose`; hidden `layerType=corrected_transcription`, `sourceMethod=manual_entry` (no supersedes_layer_id); pre-filled textarea; status select with creation-time options; auto-close on success via useEffect + submitted ref pattern
- New component `UpdateLayerStatusForm`: props `layerId`, `currentStatus`, `canUpdate`; renders null if `canUpdate=false`; full 5-option status select; inline error display; compact layout
- Modified `TextLayerCard`: imports both new forms; `showTranscribeForm` state (mutually exclusive with `showVersionForm`); "Edit / Transcribe" button on `source_ocr`/`source_transcription` active layers; `UpdateLayerStatusForm` on all active layers above action row
- Review P1 fix: added `NEW.id IS DISTINCT FROM OLD.id` to immutability trigger
- Review P2 fix: removed `is_super_admin()` from UPDATE policy and `assertLayerPermission` bypass from `updateLayerStatus` — aligns write boundary with membership-only SELECT policy; documented in both migration and action
- Build, typecheck, lint all pass. All 5 PRD test steps satisfied. F012 PASSED.
- plan/2026-03-13-f012-transcription-editor-review-status.md written

### Session 12 — 2026-03-13
- F008: Wits Supplementary Source Intake — implemented via parallel agent team (2 agents), squash merged to main
- No DB migration required — source_type='wits' already in ENUM; source_identifier, source_url, file_assets XOR constraint all in place from F006
- New adapter: src/lib/sources/wits.ts — XMLParser (fast-xml-parser), validateWitsRef (OAI id format guard), toStringArray/toStringOrNull normalisers (Addendum 3: explicit single-vs-array handling for dc: fields), extractDate (strict ISO first, year fallback → YYYY-01-01, date_raw always verbatim from source), extractFileUrl (file-extension scan on dc:identifier[]), fetchWitsItem (AbortSignal.timeout 10s, typed errors, no throw)
- New server action: src/lib/actions/import-wits.ts — assertImportPermission (super_admin bypass + role check), witsRef format validation, idempotency pre-check, date rejection if date_extracted null, source_records insert with source_type='wits' source_archive='Wits' date_issued=date_extracted date_issued_raw=verbatim dc:date (Addendum 2), canonical ref collision retry r2-r9 (Addendum 4: collision is expected for year-range dates), file_assets insert only if file_url non-null (Addendum 1: metadata-only path skips asset insert), no text_layers, compensating rollback on asset failure
- New import page: /projects/[id]/import/wits — server component, role gate, inline access-denied for translators/reviewers
- New form: WitsImportForm.tsx — useActionState, OAI identifier input, placeholder/hint text per spec, success/error/pending states
- Modified: /projects/[id]/page.tsx — "Import from Wits →" link added with same researcher/admin gate
- Live OAI-PMH contract verified: historic_99960 has dc:date "1955 - 1959" (range); dc:identifier = landing pages only (no file URLs); metadata-only path is standard
- fast-xml-parser added to dependencies
- Build, typecheck, lint all pass
- plan/2026-03-13-f008-wits-supplementary-source.md written (includes addendums)
- F008 PASSED.

### Session 11 — 2026-03-13
- F011: Text Layer Management — implemented via parallel agent team (3 agents, Tasks 1+2 parallel, Task 3 sequential), code reviewed (3 findings), 2 commits, squash merged to main (PR #10)
- No DB migration — existing text_layers table with supersedes_layer_id already in place from F005
- New component: src/components/records/TextLayerCard.tsx — "use client"; dedicated LAYER_TYPE_LABELS and LAYER_STATUS_STYLES maps (separate from record status helper); collapse/expand toggle (300-char preview); "Create New Version" button (canAddLayer && !isSuperseded gate); inline CreateLayerVersionForm via useState; isSuperseded dims card and shows "Superseded" badge
- New component: src/components/records/CreateLayerVersionForm.tsx — "use client"; useActionState + isPending; layer_type read-only (pre-filled from parent); supersedes_layer_id passed as hidden input; auto-closes on success via useEffect + submitted ref; double-submit prevented via disabled={isPending}
- Modified: src/lib/actions/text-layers.ts — extracts supersedes_layer_id from formData; validates: layer exists, same record_id, same layer_type (cross-type version chains rejected); supersedes_layer_id included in audit log
- Modified: src/app/(app)/projects/[id]/records/[recordId]/page.tsx — replaces <table> with TextLayerCard list; derives supersededIds via reverse lookup (new Set of supersedes_layer_id values); active layers rendered first; superseded layers in "Superseded Versions" section; sort descending by created_at
- Review findings addressed: (P1) layer_type equality check added to supersession validation; (P2) isPending guard added to CreateLayerVersionForm; (P2) supersedes_layer_id added to success log
- All 5 PRD test steps satisfied. Build, typecheck, lint all pass.
- plan/2026-03-13-f011-text-layer-management.md written (includes addendums)
- F011 PASSED.

### Session 10 — 2026-03-13
- F010: OCR and Source Text Acquisition Layer — implemented, code reviewed (1 P1 fix), squash merged to main (PR #9)
- No DB migration required — existing text_layers table + types (source_ocr, file_extract) already support this
- New utility: src/lib/sources/text-extractor.ts — uses pdf-parse v2 PDFParse class; discriminated ExtractResult type (ok/unsupported_mime_type/empty_content/parse_error); internal errors only logged, never surfaced to caller
- New server action: src/lib/actions/extract-text.ts — derives projectId from record (no client trust); V1 rule: first uploaded PDF only (logs if multiple); content-hash no-op (sha256 compare avoids duplicate rows for unchanged PDFs); supersedes_layer_id for versioning; all internal errors sanitized to fixed safe strings; revalidatePath after insert
- New component: src/components/records/ExtractTextSection.tsx — "use client"; useActionState; renders notice for non-PDF assets; renders button for PDF assets with canExtract gate; success shows layer ID; re-extract hint when source_ocr already exists
- Record detail page wired: hasPdfAsset computed from typedAssets; hasExistingSourceOcr computed from typedLayers; ExtractTextSection rendered before AddTextLayerForm under canAddLayer gate
- No Jest setup added (addendum: too much tooling surface for V1); feature verified via typecheck + build + manual test
- blobToBuffer not exported (addendum: inlined as Buffer.from(await blob.arrayBuffer()))
- Review P1 fix: removed super_admin bypass from assertLayerPermission — SELECT policies on source_records, file_assets, text_layers, and storage.objects are all membership-only; app-layer boundary now matches DB boundary
- Note: same super_admin bypass bug exists in text-layers.ts#addTextLayer (F005 code) — deferred, out of scope for this PR
- Build, typecheck, lint all pass.
- plan/2026-03-13-f010-ocr-source-text-acquisition.md written
- F010 PASSED.

### Session 9 — 2026-03-13
- F009: Source File Viewer — implemented via parallel agent team (2 agents), squash merged to main (PR #8)
- No DB migration required — extends existing record detail page
- New component: `src/components/records/FileViewerSection.tsx` — "use client"; table + inline viewer panel; PDF iframe, non-PDF fallback link, disabled View when no preview available
- New type: `EnrichedFileAsset = FileAsset & { view_url: string | null; view_url_error?: string | null }`
- Signed URLs generated server-side (1h expiry) for storage_path assets; source_url assets passed directly; isSafeUrl() enforces http/https-only constraint
- Viewer panel: attaches below table visually (border-t-0), header with filename + asset_type badge + Close button, iframe for application/pdf, fallback for all other types
- Failure handling: createSignedUrl errors → view_url_error → disabled button; null view_url → disabled button
- Signed URL expiry acknowledged: broken iframe state after 1h is acceptable (no auto-refresh)
- Tenancy invariant preserved: existing requireProjectMember guard covers viewer (no weakening)
- All 5 PRD steps satisfied. Build, typecheck, lint pass.
- Review hardening: error?.message → "Preview unavailable" (no internal strings to client); isTrustedOrigin() allowlist gates iframe to Supabase/NLSA/Ibali; untrusted-origin PDFs fall back to open-link
- plan/2026-03-13-f009-source-file-viewer.md written
- F009 PASSED.

### Session 8 — 2026-03-13
- F007: NLSA Source Integration — implemented via parallel agent team (2 agents), code reviewed (P1 fix: tightened date validation from partial to full YYYY-MM-DD), squash merged to main (PR #7)
- No DB migration — F006 schema (source_identifier, source_url, nullable storage_path, location CHECK) already covers NLSA
- New adapter: src/lib/sources/nlsa.ts — ContentDM flat-field mapping (title/date/langua/descri/find/fulltext), fetchNlsaItem, mapNlsaItem, extractNlsaRefs, AbortSignal.timeout(10_000), typed NlsaFetchResult
- Server action: importFromNlsa — strict nlsa_ref parser (two accepted shapes: {alias}/{id} or full ContentDM URL; everything else rejected with descriptive error), alias normalised to lowercase, full YYYY-MM-DD required (YYYY/YYYY-MM rejected — would produce malformed canonical refs), idempotency pre-check, source_type="nlsa" source_archive="NLSA", asset_type="source_file", layer_type="source_ocr", compensating rollback on partial failure
- Review P1 fix: date regex tightened from /^\d{4}(-\d{2}(-\d{2})?)?$/ to /^\d{4}-\d{2}-\d{2}$/ — partial dates violate CanonicalRefFields.date_issued // YYYY-MM-DD contract and the date column type
- Import page: /projects/[id]/import/nlsa with role gate; translator/reviewer see inline access-denied (server-side invariant also enforced in action)
- NlsaImportForm: useActionState, placeholder p21048coll37/1, hint text listing known aliases (p21048coll37 Imvo Zabantsundu, p21048coll77 Lentsoe la Basotho)
- Project detail: "Import from NLSA →" link added next to Ibali link (same researcher/admin gate)
- Provenance distinction: layer_type="source_ocr" kept explicit and separate from Ibali's "source_transcription"
- All 5 PRD steps satisfied. F007 PASSED.

### Session 7 — 2026-03-13
- F006: Ibali Source Integration — implemented via parallel agent team (3-wave, 5 agents), code reviewed (1 P1 fix), squash merged to main (PR #6)
- Migration `20260313000003`: source_identifier + source_url on source_records; partial unique index (dedupe invariant); source_url on file_assets; storage_path made nullable; location CHECK constraint (exactly one of storage_path/source_url per row)
- New adapter: src/lib/sources/ibali.ts — Omeka S property helper, typed fetch results, 10s AbortSignal timeout, 3 error shapes (not_found/timeout/network)
- Server action: importFromIbali — idempotency pre-check, assertImportPermission (mirrors existing pattern), canonical ref collision retry, compensating rollback on any partial child failure, accepts raw ID or full Ibali URL
- Review P1 fix: removed new Date() fallback from canonical ref generation; import now rejects items with no dcterms:date rather than inventing provenance
- Import page: /projects/[id]/import/ibali with role-gated access; translator/reviewer see inline access-denied (not a redirect)
- IbaliImportForm: useActionState, success shows record link, error inline
- Project detail: Import from Ibali link (same gate as Upload)
- Types: SourceRecord extended with source_identifier/source_url; FileAsset storage_path nullable, source_url added
- supabase db push applied; build passes clean
- All 5 PRD steps + 4 addendum criteria (idempotency, location invariant, compensating rollback, timeout/error shaping) satisfied. F006 PASSED.

### Session 6 — 2026-03-13
- F005: Canonical Record Creation — implemented via parallel agent team (3 agents), code reviewed, 2 fixes applied, squash merged to main (PR #5)
- Migrations: 20260313000001_f005_text_layers.sql (table, idempotent enums, RLS SELECT/INSERT, updated_at trigger); 20260313000002_f005_drop_text_layers_update_policy.sql (compensating: drops UPDATE policy flagged in review)
- Types: TextLayer added to src/types/index.ts
- Server action: src/lib/actions/text-layers.ts — addTextLayer; derives project_id from source_records (not client field); permission guard (project_admin/researcher only, translator/reviewer denied); audit log; revalidatePath
- Detail page: /projects/[id]/records/[recordId] — combined tenancy guard (project_id + recordId in one query), provenance read-only panel, file assets table, text layers table with type/status badges, AddTextLayerForm gated by role
- Component: AddTextLayerForm — useActionState, layer_type select, content textarea, optional language, source_method select
- Records list: canonical_ref cell links to detail page
- Review P1 fix: UPDATE policy removed — content is immutable; new versions use supersedes_layer_id; no F005 UI uses UPDATE; policy was not scoped to current project membership
- All 5 PRD test steps satisfied. F005 PASSED.

### Session 1 — 2026-03-12
- Initial project docs, PRD, feature list, architecture docs created
- Source integrations researched and validated

### Session 2 — 2026-03-12
- F001: Authentication and Role-Based Access — implemented, reviewed (3 rounds), merged
- SQL migration applied; both users provisioned; Vercel env vars set

### Session 5 — 2026-03-12
- F004: Upload Metadata Enforcement and File Naming — implemented via parallel agent team (6 agents, 2 waves), PR reviewed (1 P1 fix + retry loop fix), squash merged to main
- Migrations: 20260312000003_f004_canonical_ref.sql (2-phase backfill: nullable → UPDATE LEGACY-{id} → NOT NULL → UNIQUE INDEX); 20260312000004_f004_canonical_ref_fix.sql (live-DB fixup for LEGACY sentinel collision bug caught in review)
- New file: src/lib/records/canonical-ref.ts — pure TS, no directive, shared server+client; generateCanonicalRef(), appendCollisionSuffix(), MAX_COLLISION_RETRIES
- canonical_ref format: {SOURCE}-{PUB}-{YYYY-MM-DD}[-v{vol}][-i{iss}][-p{page}][-a{article}]
- Known source alias map: READEX, IBALI, NLSA, WITS; known pub aliases: IMV, ISIG, LB; deterministic fallback normalization for unknown publications
- Server action: date_issued now required; new fields volume/issue_number/article_label extracted; canonical_ref generated and stored with collision retry (up to -r9)
- UploadForm: required date, 3 optional new fields, live canonical ref preview using shared helper
- Records list: canonical_ref column added (font-mono, em-dash fallback)
- SourceRecord type extended: volume, issue_number, article_label (nullable), canonical_ref (string)
- Code review P1 finding: DEFAULT 'LEGACY' under unique index collides with >1 existing row — fixed with 2-phase migration + fixup migration; retry loop off-by-one fixed (was ≤10, now ≤9)
- All 5 PRD test steps satisfied. F004 PASSED.
- PR #4, commit on main

### Session 4 — 2026-03-12
- F003: Manual Archive Upload Intake — implemented via parallel agent team, PR reviewed (2 rounds), squash merged to main
- Migrations: 20260312000001_f003_source_records.sql (tables, enums, RLS, bucket) + 20260312000002_f003_fixes.sql (compensating cleanup DELETE policies, super_admin RLS alignment, is_super_admin() helper)
- Action: uploadRecord — pre-generated UUID, compensating transaction (storage cleanup on DB failure), explicit role guard, server-side file validation, upsert: false
- Types: RecordStatus, SourceRecord, FileAsset
- Pages: /projects/[id]/upload (role-gated), /projects/[id]/records (table, status badges, empty state)
- Project detail: records count + view/upload links
- PR #3 — two P1 findings from code review (compensating cleanup blocked by missing DELETE policies; super_admin RLS gap) — both fixed and re-merged
- All 5 PRD test steps verified. F003 PASSED.

### Session 3 — 2026-03-12
- F002: Project and Team Management — implemented, reviewed, fixed, merged
- Backend: migration (projects + project_memberships, SECURITY DEFINER RLS helpers, triggers)
- Types: Project, ProjectMembership, ProjectStatus
- Guard: requireProjectMember (non-members redirect)
- Actions: createProject, addMember, updateMemberRole, removeMember
  — orphan guard on both role demotion and removal
  — explicit server-side admin assertion (returns { error } not throws)
  — audit log with actor + affected user_id on all mutations
- Pages: /projects, /projects/new, /projects/[id]
- Components: ProjectForm, AddMemberForm, MemberRoleForm, RemoveMemberForm
- Sidebar: Projects nav link
- supabase db push applied to remote
- PR #2 reviewed (4 findings: FK disambiguation, auth throws→returns, unused helper, audit user_id), all fixed, squash merged
